'use strict';

/* Controllers */
// Alarm controller
app.controller('Alarm_detailController', [
    '$scope',
    '$http',
    '$state',
    function ($scope, $http, $state) {
        // 初始化
        $scope.init = function (params) {
            
        };
     

        $scope.init();
    },
]);